#include "main.h"

/**
 * print_line - draws line according to parameter set
 * @n: The number of lines to draw
 *
 * Return: 0
 */

void print_line(int n)
{
	int a;

	if (n <= 0)
	{
		_putchar('\n');
	}
	else
	{
		for (a = 0; a < n; x++)
		{
			_putchar(95);
		}

		_putchar('\n');
	}
}
